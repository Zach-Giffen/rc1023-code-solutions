/* exported take */
// create a function calling two arguments, array and count
// return the function with a slice method starting at the count

function take(array, count) {
  return array.slice(0, count);
}

// create a function calling two arguments, array and count
// return the function with a slice method starting at the o index, and going to count
