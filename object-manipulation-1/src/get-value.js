/* exported getValue */
// make ampty array
// check for the key in an object
// push the key to the array
// return array

function getValue(object, key) {
  return object[key];
}

// return the object at the key value
